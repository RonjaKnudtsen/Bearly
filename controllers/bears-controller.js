angular.module('bearly.controllers').controller('bears-controller', function($scope, $stateParams, $state, $firebaseObject, ReviewService, DrinkService){
	
	/*$scope.reviews = ReviewService.getReviews();
	console.log($scope.reviews);

	$scope.reviews.$loaded().then(function(){
		$scope.drinks = DrinkService.getDrinks();
		console.log($scope.reviews.length);
	}.bind(this));

	console.log($scope.reviews);*/
	//$scope.drink = DrinkService.getDrink($scope.review.drinkId);
});